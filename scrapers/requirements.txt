# Core scraping (static HTML)
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Optional: JS-heavy sites (Facebook Marketplace, some apartment portals)
# Uncomment and run: pip install stagehand playwright && playwright install chromium
# stagehand>=3.5.0
# playwright>=1.40.0
